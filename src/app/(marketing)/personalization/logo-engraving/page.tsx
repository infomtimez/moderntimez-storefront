import { PageShell } from "@/components/layout/PageShell";

export default function LogoEngravingPage() {
  return (
    <PageShell
      eyebrow="Personalization"
      title="Logo Engraving"
      intro="Add a company logo, team mark, or event artwork to select gifts and awards. Clean vector files are ideal, and we can help assess artwork before production."
    />
  );
}

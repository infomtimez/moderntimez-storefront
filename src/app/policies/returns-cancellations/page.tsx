import { PageShell } from "@/components/layout/PageShell";

export default function ReturnsPolicyPage() {
  return (
    <PageShell
      eyebrow="Policies"
      title="Returns & Cancellations"
      intro="Because personalized items are made to order, return and cancellation eligibility may differ from non-custom goods. Contact support as soon as possible if an order needs attention."
    />
  );
}

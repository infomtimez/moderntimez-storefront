import { PageShell } from "@/components/layout/PageShell";

export default function ShippingPolicyPage() {
  return (
    <PageShell
      eyebrow="Policies"
      title="Shipping Policy"
      intro="Shipping timelines depend on the item, engraving complexity, and destination. Final production and delivery estimates should be confirmed before checkout or during quote review."
    />
  );
}
